<main role="main" class="container is-none-image">
  <div class="my-3 p-3 bg-white rounded shadow-sm">
    <h6 class="border-bottom border-gray pb-2 mb-0">Sugestões</h6>
    <% @deputies.each do |d| %>
      <div class="media text-muted pt-3">
        <img data-src="holder.js/32x32?theme=thumb&bg=007bff&fg=007bff&size=1" alt="" class="mr-2 rounded">
        
        <div class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
          <div class="d-flex justify-content-between align-items-center w-100">
            <strong class="text-gray-dark"><%= d.full_name %></strong>            
            <%= link_to "Seguir", follow_deputy_path(d.id), method: :post %> 
          </div>
          <span class="d-block"><%= d.political_party %></span>
        </div>
      </div>
    <% end %>
  </div>
</main>
