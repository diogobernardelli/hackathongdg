<div class="container">
  <div class="form-group row">
    <%= @deputy.full_name %>
    <%= @deputy.political_party %><br />
  </div>

  <div class="form-group col-4">
    <%= link_to "Seguir", follow_deputy_path(@deputy.id), method: :post %>
    <%= image_tag (@deputy.url_photo) %>  
  </div>
</div>
